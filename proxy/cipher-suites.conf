# Client (proxy) supported cipher suites, in order of preference.
# Format: [SuiteName] followed by the crypto config for that suite.

[SHP_AES_256_GCM]
CONFIDENTIALITY:AES/GCM/NoPadding
INTEGRITY:NULL
